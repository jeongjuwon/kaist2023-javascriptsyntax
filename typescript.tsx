import {FC} from 'react';

type Props = {
  id: number;
  content: string;
  size: number;
};

// FunctionComponent
const Toast: FC<Props> = ({id, content, size}) => {
  return null;
};

export default Toast;
